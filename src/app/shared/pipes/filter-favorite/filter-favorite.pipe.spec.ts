import { FilterFavoritePipe } from './filter-favorite.pipe';

describe('FilterFavoritePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFavoritePipe();
    expect(pipe).toBeTruthy();
  });
});
